name: 17 - 2 - Custom actions - JS
run-name: 17 - 2 - Custom actions - JS | env - ${{ inputs.target-env }}

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Checck for dependency updates
        uses: ./.github/actions/composite-cache-deps